<div class="container-fluid">
  <div ng-controller="MapController as map">
    <h1>{{map.userLocationArea.results[0].formatted_address}}</h1>

    <%= render "modal_request.html.erb" %>

    <div id="map"></div>

  </div>
  <section class="request-info-box" ng-controller="RequestController as r">
  <%= render "single_request_info.html.erb" %>
  <section class="request-info-box">
</div>
