<div class="card hovercard">

    <div class="cardheader">
    </div>

    <div class="avatar">
      <img class="img-circle" ng-src="{{r.requestUser.avatar_url}}" alt="Profile picture">
    </div>

    <div class="info">

        <div class="title">
          <h1> {{r.requestUser.f_name}} {{r.requestUser.l_name}}</h1>
          <h3 ng-show="r.clickedRequest.group_size"> ...and {{r.clickedRequest.group_size > 1 ? r.clickedRequest.group_size + ' others' : 'a friend'}}</h3>
          <p> {{ r.clickedRequest.description }} </p>
        </div>

        <div class="desc">
          Is looking to do something in<b> {{ r.clickedRequest.location }} </b>
        </div>

        <div class="desc">
          On
          <b>
            {{r.clickedRequest.request_date | date:'fullDate'}}
          </b>
        </div>

        <h4> Other details to bear in mind: </h4>

        <div class="desc">
          {{r.requestUser.f_name}} has <b> £{{r.clickedRequest.budget}}</b> to spend
        </div>

        <div class="desc">
          <h5> {{r.requestUser.f_name}}'s availability:
            <b>{{ r.clickedRequest.time_of_day === 'All day' ? 'All day' : 'in the ' + r.clickedRequest.time_of_day}}</b>
          </h5>

          <h5 ng-show="r.clickedRequest.duration">and is available for
            <b>{{r.clickedRequest.duration}} hours</b>
          </h5>

          <h5 ng-show="r.clickedRequest.tour_time_start">
            From:  {{r.clickedRequest.tour_time_start | date:'HH:mm'}}
          </h5>

          <h5 ng-show="r.clickedRequest.luggage">
            {{r.requestUser.f_name}} has
            <b> heavy luggage in tow</b>
          </h5>

          <h5 ng-show="r.clickedRequest.children">
            {{r.requestUser.f_name}} has
            <b> children who will be on the tour </b>
          </h5>

          <h5 ng-show="r.clickedRequest.airport_access">
            {{r.requestUser.f_name}} has
            <b> to be able to get to the airport </b>
          </h5>
          <h5 ng-show="r.clickedRequest.disability_access">
            {{r.requestUser.f_name}} needs
            <b> disability access </b>
          </h5>

        <h3> Think you can help <b>{{r.requestUser.f_name}}</b> ? </h3>

    </div>

    <div class="bottom" ng-hide="r.requestUser.id == r.current_user_id">

      <%= link_to "/requests/{{r.clickedRequest.id}}/replies/new" do %>

        <button class="btn-for-request" type="submit" ng-click="r.toggleResponseForm">Reply to {{r.requestUser.f_name}}'s request</button>

      <% end %>

      <button class="btn-for-request" type="submit" ng-click="r.toggleUserInfo">Find out more about {{r.requestUser.f_name}}</button>

    </div>

</div>
